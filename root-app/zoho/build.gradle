dependencies {
    implementation project (':framework')
    implementation project (':root-app')
    implementation files (project (':table-constants').tasks.named ('jar').flatMap { it.archiveFile })
}